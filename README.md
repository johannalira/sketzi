# sketzi

> Uma app feita com Expo + React Native / TypeScript — boilerplate pra começar a desenvolver apps mobile rápido — e servir como base para projetos futuros.

## 📌 O que é

O **sketzi** é um projeto inicializado com Expo via `create‑expo‑app`, destinado a servir como estrutura base para apps mobile. Ele provê configuração pronta para rodar no Android, iOS ou web via Expo.  

Ideal pra quem quer começar um app do zero sem setup pesado ou pra usar como base de estudos/protótipos.

## ⚙️ Tecnologias usadas

- React Native + TypeScript  
- Expo  
- JavaScript / TypeScript  
- Outras libs/configurações definidas no `package.json`  

## 🚀 Como rodar localmente

1. Clone este repositório:  
   ```bash
   git clone https://github.com/johannalira/sketzi.git
